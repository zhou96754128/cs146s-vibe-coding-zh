# C1 课程资料获取与翻译 · 七维 AAR 复盘

> 本复盘基于本项目真实产物整理，每条结论都能在仓库里核对：`AI日志/AI日志.md`、`out/qc-report.md`、`out/qc-struct-audit.md`、`glossary/术语表.md`、`pipeline/`、`translated/`。
> 周期：2026-09-15 ~ 2026-09-16（密集约 2 天，压缩了原建议的 4 天）。

---

## 一、我学到了什么（具体概念/方法/技能）

1. **先读 rubric 再定策略**：100 分里管线自动化 20 + AI 使用质量 20 = **40 分与"译得好不好"无关**。这个判断直接决定了做法——先把管线搭对、把过程留痕，再谈逐句翻译质量。
2. **术语表必须"先冻结、再翻译"**：让 AI"边翻边保持术语一致"在长文上必然漂移（同页内都会前后不一）。把术语抽成**独立、可机器校验的锚点表**先冻结，是保证一致性的唯一可行做法。
3. **长文翻译的正确单位是"语义分片"，不是"页"**：5 万字节级的页面必须按语义边界切分、逐片落盘、最后合并并核对锚点行——分片落盘是可追溯的前提。
4. **抽检脚本的误报要和译文错误同等治理**：判据太粗会制造假警报；"改译文去迎合错误判据"是最坏的做法。误报不治理，报告就没人信。
5. **具体技术方法**：HTML→Markdown 抽取时在源头剥离 `nav/header/footer/aside/form` 样板块；还原被转义的 HTML 实体前，先用"源侧同类实体计数为 0"证明其为翻译伪影；代码围栏要按缩进识别并跳过，不能当正文。

## 二、我的流程

| 阶段 | 时间投入 | 做了什么 | 卡点 |
|---|---|---|---|
| 读懂任务 | 0.5 天 | 读 `CHALLENGE.md`/`rubric.json`；解压 `materials/CS146S_offline.zip` 盘点资产 | 一度误以为重点是"翻译 31 页"产量 |
| 建术语表 | 0.5 天 | 扫全文抽高频概念 → 94 条分组锚点表 | 前两版 prompt 产出泛泛、不可校验 |
| 搭流水线 | 0.5 天 | `pipeline/extract.py`（HTML→MD）+ `pipeline/pipeline.py`（extract/coverage/glossary） | 首版把侧栏导航也抽进来了 |
| 翻译主体 | 1 天 | 逐页翻译至 26/31 | 超长页读入溢出、内容缺块 |
| 攻克长页 | 0.5 天 | 分片→逐片翻译→合并→核对锚点行，达成 100% | 分片边界与合并完整性 |
| 质量抽检 | 0.5 天 | `pipeline/qc.py` 四类检查 + 逐条对账 + 硬伤修复 | 13 项结构差异/残留英文的定性 |

## 三、我与 AI 的协作

- ✅ **好指令**：「先别翻，先告诉我这批材料里有哪些文件、各自多大、哪些明显是空的」——先体检后手术，避免了对着 0 字节空壳页做无用功。
- ✅ **好指令**：「只列这门课**特有且容易译歪**的概念，给统一译名，并注明哪些必须保留英文」——把"泛泛词表"变成"可校验锚点表"。
- ❌ **差指令**：「帮我列一份术语表」（v1）→ 产出全是通用词，对一致性毫无帮助。
- ❌ **差指令**：「直接读这一页并翻译」（对超长页）→ 读回内容中间缺块，若硬翻必然漏译或编造。
- 🔄 **迭代案例**：术语表 prompt 走了 **3 版**（泛泛清单 → 加统一译名 → 按类目分组+保留英文规则）才收敛；质检脚本同样走了多轮（首版误报 → 加代码围栏跳过 → 加逐字引用白名单 → 收紧叠字判据）。

**协作分工**：AI 负责"做"（抽取脚本、翻译、质检脚本、修复脚本），我负责"定标准、判真假、拿主意"。整条链上最关键的岗位不是翻译，而是**判断"这个告警到底算不算问题"**。

## 四、我完成了什么

| 交付物 | 最终状态 |
|---|---|
| `README.md` | 资料包说明：来源、覆盖范围、翻译流程、使用方法、已知缺口 |
| `AI日志/AI日志.md` | 3 天逐环节记录：工具、prompt 迭代、踩坑与改法 |
| `AAR复盘.md` | 本文件（七维） |
| `拿来说明/`（3 份） | 术语表冻结、超长页分片翻译、质检脚本与误报治理 |
| `translated/` | 32 个译文文件，**页数覆盖度 100%**（31/31），译文约 9.8 万中文字符 |
| `glossary/术语表.md` | 94 条（要求 ≥50） |
| `pipeline/` | `extract.py` / `pipeline.py` / `qc.py`，可复跑 |
| `out/qc-report.md` | 质检终态：结构 0 ｜ 残留英文 0 ｜ 术语 0 ｜ 缺译文 0 |

**自我评价**：管线维度（40 分）有把握；翻译准确度（25 分）主体可靠，`agentic-ai-threats` 这类超长安全页仍有"机翻味"的句子，需要继续人工润色。

## 五、卡点与突破

- **最大卡点**：`agentic-ai-threats.md`（源文约 5.3 万字节）没法一次读入翻译。
- **错误应对**（值得记录）：第一反应是"分几轮读同一页、拼起来翻"，结果轮次之间内容重叠、边界不清，反而更不可控。
- **正确解法**：先按语义边界把超长页切成 `chunks/<stem>__pNN.md` 落盘，逐片翻译到 `translated/_parts/`，再按序合并为 `translated/<stem>.md`，最后逐条核对 10 个攻击场景/每张表格标题这类**锚点行**是否齐全。合并后 37,723 字节 / 523 行，锚点逐项对齐，覆盖度到 100%。
- **"真正进脑子"的瞬间**：意识到覆盖度不是靠"读得多"证明的，而是靠**分片产物 + 锚点核对**证明的——能被检查的完整性，才是完整性。

## 六、改进方向（下次同类挑战）

1. **第一天就把评分权重拆成"可交付检查项"**，按分值排优先级，而不是按"感觉哪里难"排。
2. **术语表先冻结再动手翻译**，并写一个脚本检查"译文中术语是否全部命中标准译名"（本次是事后才补的一致性校验）。
3. **超长文档从一开始就分片**，并把"锚点清单"（每页应有的标题/表格数）作为翻译的前置输入。
4. **抽检脚本先写好、再干活**：至少在翻完第一页后就跑一次，避免最后集中处理大量误报。
5. **发布前预留一步"陌生环境试跑"**：把 README 里的命令在一台干净环境里走一遍，确认陌生人能独立复现。

## 七、KSTAR 学习信号

- **R̂（预期）**：4 天完成"抓取 → 抽取 → 翻译 → 质检 → 发布"，覆盖度 ≥80%，把管线跑通即可。
- **R（实际）**：约 2 天完成到覆盖度 100%、质检四项归零；**发布环节一度受阻、最终改道拿下**——本机 `git` 依赖 Xcode 开发者工具、当前不可用，`spawn workbuddy ENOENT` 导致外包翻译也失败；改走 GitHub 官方 REST API 直推（新建公开仓库、上传 167 个文件 / 22.3 MB，提交 `fe0c250`），交付物已公开可访问。
- **ΔR 归因**：
  - **K（知识）**：加分——"先读 rubric 定策略""术语表先冻结"两条判断直接省下大量返工。
  - **T（任务理解）**：初期偏差——把"翻译"当主线，漏看了管线/AI 质量占 40 分，靠读 rubric 纠正。
  - **S（技能）**：加分——分片+合并、锚点核对、实体还原判据，是可迁移的通用技能。
  - **Â（环境/工具）**：减分——本机缺少 git 开发者工具与 workbuddy 可执行文件，把"发布"从顺手一步变成需要外部条件的阻塞项（后经 GitHub REST API 直推绕过，但为绕路多花了排查时间）。
- **K′（下次带入的新知识）**：
  1. 长文本任务**默认先分片**，并把锚点清单前置；
  2. 任何"自动化检查"上线前都要**先喂一批已知正确样本**验证判据，避免误报治理浪费；
  3. 涉及"发布/外部账号"的交付物，**第一天就确认环境依赖**（git/CLI/凭据），不要把它留到最后。
